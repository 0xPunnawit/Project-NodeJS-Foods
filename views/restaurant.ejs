<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/styles/navbar.css" rel="stylesheet">
  <title><%= title %></title>
  <style>
    .restaurant-card {
      margin-bottom: 20px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      border-radius: 10px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .restaurant-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
    }
    .restaurant-card .card-header {
      background-color: #f8f9fa;
      font-weight: bold;
      text-align: center;
    }
    .status-open {
      color: green;
      font-weight: bold;
    }
    .status-closed {
      color: red;
      font-weight: bold;
    }
    .btn-more {
      background-color: #007bff;
      color: #fff;
      font-weight: bold;
      width: 100%;
      border-radius: 5px;
    }
    .btn-more:hover {
      background-color: #0d70da;
    }
  </style>
</head>
<body>
  <!-- เรียกใช้ Navbar -->
  <%- include('partials/navbar') %>
  <br>
  <main class="container mt-5">
    <!-- เนื้อหาหลักของหน้า -->
    <h1 class="mb-4 text-center">ร้านอาหาร</h1>

    <div class="row">
      <% restaurants.forEach(restaurant => { %>
      <div class="col-12 col-sm-6 col-md-4 mb-4">
        <div class="card restaurant-card">
          <div class="card-header">
            <%= restaurant.name %>
          </div>
          <div class="card-body">
            <p>
              <strong>สถานะ:</strong> 
              <span class="<%= restaurant.status === 'เปิดร้าน' ? 'status-open' : 'status-closed' %>">
                <%= restaurant.status === 'เปิดร้าน' ? 'เปิดร้าน 🟢' : 'ปิดบริการ 🔴' %>
              </span>
            </p>
            <% if (restaurant.status === 'เปิดร้าน') { %>
              <a href="/restaurant/<%= restaurant.restaurant_id %>" class="btn btn-more mt-3">เข้าชม</a>
            <% } else { %>
              <button class="btn btn-secondary mt-3" disabled>ไม่สามารถเข้าชมได้</button>
            <% } %>
          </div>
        </div>
      </div>
      <% }) %>
    </div>
    
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
